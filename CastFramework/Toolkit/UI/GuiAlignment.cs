namespace CastFramework
{
    public enum HAlignment : byte
    {
        Left = 0,
        Center, 
        Right,
        Stretch
    }

    public enum VAlignment : byte
    {
        Top = 0,
        Center,
        Bottom,
        Stretch
    }
}